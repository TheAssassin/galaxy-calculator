# The Trusty beta Build Environment
sudo: required
dist: xenial

install:
  - sudo aptitude install debian-keyring debian-archive-keyring
  - sudo add-apt-repository ppa:beineri/opt-qt-5.12.7-xenial
  - sudo apt-get update
  - sudo apt-get install -y build-essential g++ qt5-default qttools5-dev-tools qt5-qmake quickcontrols2 qtdeclarative5-dev # install necessary Qt files

script:
#  - qmake "CONFIG+=test" GalaticCalculatorLinux.pro # test mode 
#  - make
#  - ./GalaticCalculator # run unit test
#  - rm Makefile GalaticCalculator *.o # remove files (which are generated from unit test) for next build
  - qmake "CONFIG+=release" GalaticCalculatorLinux.pro # we gonna compile for actual program
  - make
  - ./GalaticCalculator # run actual program
# End of File
